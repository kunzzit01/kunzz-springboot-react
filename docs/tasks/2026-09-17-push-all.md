# 任务 2026-09-17-push-all

- 状态：进行中
- 分支：main
- 白名单（本任务意图就是「把所有未提交的东西固化」，故按目录授权）：
  - inventory-system/frontend/src/
  - backend/src/main/java/com/kunzz/inventory/service/FreezerCategoryService.java
  - backend/src/main/java/com/kunzz/inventory/controller/FreezerCategoryController.java
  - backend/static/
  - backend/target/
  - CHANGELOG.md
  - docs/
  - .gitignore
- 明确不碰 / 不提交：
  - .zcode/           # 会话本地工作目录（备份、计划），已加进 .gitignore，不入库
  - docs/docs/        # 意外产生的嵌套重复目录（10 个文件与 docs/ 同名），不提交，待人工删除
- 备注：
  - 分 4 个提交：① 冰箱分类去「停用」重构 ② 样式修复 ③ 构建产物同步 ④ 文档。
  - 提交前验证：前端 `tsc -b` 退出码 0；后端 `target/classes` 的 .class（17:57:50）
    比 .java（17:56:47）新，说明当前源码编译通过。
  - `git status` 在本仓库会误报约 113 个 OneDrive 仅在线占位文件，全程用 `git diff` 判断真实改动。
