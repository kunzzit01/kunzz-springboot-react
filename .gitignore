# ---------- Node / React ----------
node_modules/
dist/
*.tsbuildinfo

# ---------- Java / Maven ----------
backend/target/
*.class
*.jar
!backend/target/*.jar
.mvn/wrapper/maven-wrapper.jar

# ---------- IDE ----------
.idea/
*.iml
.vscode/
*.swp

# ---------- 运行时与本地环境 ----------
runtime/
jre*/
mariadb/

# ---------- 日志 ----------
*.log

# ---------- 数据库 ----------
# 自动备份/历史快照不入库；主数据包单独纳入（一键启动需要）
database/*
!database/u690174784_kunzz.sql
*.sql.bak

# ---------- 用户上传数据 ----------
# 碗碟照片需要随仓库分发（新用户直接可用），故纳入版本库；
# 若后续图片过多导致仓库过大，可改为只推送 backend/uploads/dishware/ 或使用 Git LFS
# backend/uploads/

# ---------- live 同步脚本凭证（绝不可推送，仓库是公开的） ----------
live-credentials.json
inventory-system/frontend/live-credentials.json
sync-live-stock*.log

# ---------- 系统文件 ----------
Thumbs.db
.DS_Store
desktop.ini

# ---------- Maven 本地仓库（误建清理） ----------
.m2/
